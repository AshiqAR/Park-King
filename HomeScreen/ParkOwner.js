import React from "react";
import { Text, View, StyleSheet, TouchableOpacity, Image } from "react-native";

const ParkOwner = ( {navigation}) => {
    return(
        <View>
            <Text>Park owner page</Text>
        </View>
    )
};

export default ParkOwner;
